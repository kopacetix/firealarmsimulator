import React from "react";
import ReactDOM from "react-dom/client";
import FireAlarmTrainer from "./FireAlarmTrainer.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FireAlarmTrainer />
  </React.StrictMode>
);
